== README

still very much work in progress

=== Frontend
from / you can access different views of the bus network in Singapore

=== REST API

/api/buslines/{id}/busstopdetails/{id}
/api/busstopdetails/{id}

=== Data import
The data is from the LTA API

A new import can be triggered via
/busstops/generate_route
/busstops/generate_info

