<% @allstops.each do |a| %>
  <%= a["uid"] %> <br />
  <%= a["code"] %> <br />
  <%= a["road"] %> <br />
  <%= a["desc"] %> <br />
  <%= a["lat"] %> <br />
  <%= a["long"] %> <br />
  <%= a["zip"] %> <br />
  <br /><br />
  
<% end %>

